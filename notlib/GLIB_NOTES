==> DATA STRUCTURES

==> SCENARIOS

Notify called, note is created (w/ timeout)
timeout completed, note closed

Notify called, note is created (w/ timeout)
CloseNotification called, note closed

Notify called (w/ same ID as current note)
note is replaced
note is replaced again
timeout completed, note closed

CloseNotification called (note does not exist)
